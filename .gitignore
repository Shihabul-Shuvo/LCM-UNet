__pycache__/
*.py[cod]
*.pt
*.pth
*.ckpt
# Repo-root data/ is docs-only (data/PREPROCESSING.md); real data lives
# under DRIVE_ROOT (lcmunet/paths.py), never in the git repo. Scoped with a
# leading slash so it does NOT also match lcmunet/data/ (the data pipeline
# package, which must be tracked).
/data/*
!/data/PREPROCESSING.md
checkpoints/
secrets/
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/
.env
kaggle.json
.DS_Store
.lcmunet_local_root/
.pytest_cache/

docs/
kvasir-seg/
cvc_clinicdb/
